<?xml version="1.0" encoding="utf-8"?>
<!-- 代理音源接口与直链域名由用户 JSON 动态配置，无法静态枚举，故全局放行明文以支持 http 直链 -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>

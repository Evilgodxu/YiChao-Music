# 工作日志

## 2026-09-13

- 残影第二轮：首页竖屏已正常，音乐面板/横屏滚动仍见顶部细线 → 定位为视口溢出，扩大渐隐区与边缘保护带。
  - 做了什么：MusicPanelLyrics `FADE_TOTAL_LINES` 1.25 → 1.6，`verticalFadeMask` 的完全透明保护带由 `fadeFraction * 0.08` → `0.12`。上轮 0.08 保护带（约 2.8dp 全透明）未消除音乐面板顶部残影，说明残影并非「恰在边缘像素」的亚像素细线，而是容器高度不足以容纳设定行数时，顶部溢出行被 clip 成的一薄片——仅靠窄渐变/单点透明掩不掉，需让渐变区覆盖一整行、保护带覆盖整片残影。
  - 踩坑：现象——上轮加 0.08 保护带后，首页竖屏已正常（其固定高度容器恰好贴合视口、无溢出），但音乐面板（weight(1f) 容器、可能比所需行高矮）与横屏（滚动时行越界）仍在顶部见细线，且仅顶部、底部无。推断——残影由「顶部溢出行薄片」造成，非纯渐隐覆盖不足就是 DstIn 蒙层对 graphicsLayer 行在硬件合成路径上未完全生效；先以扩大渐隐区/保护带验证。
  - 验证：`./gradlew :app:compileDebugKotlin` BUILD SUCCESSFUL；待真机确认。


- 一并修复首页竖屏行数估算（同源）与歌词顶部/底部「残影」细线。
  - 做了什么：①PortraitPlayer 的 `lyricLineHeight` 由裸 `TextStyle` 测量改为 `LocalTextStyle.current.merge(TextStyle(fontSize))`（与 LyricsPanel/LyricChar 同口径，沿用 bodyLarge 的 24sp 行高），并将外层 `lyricsAreaHeight` 的行间距由 2.dp 对齐为 1.dp（与 MusicPanelLyrics 的 LYRIC_LINE_SPACING 一致），消除首页竖屏同样「设 N 行显示不足 N 行」的问题。②MusicPanelLyrics `verticalFadeMask` 上下边缘各加一段完全透明保护带（`fadeFraction * 0.08`），修复歌词区顶部/底部偶现约 1dp 的纯色细线残影。
  - 踩坑：残影现象——顶部歌词偶现约 1dp 或更小的纯色细线。原因——歌词行带 `graphicsLayer` 缩放并以小数 y 放置（`y.roundToInt()`），字形/光晕可能仅以子像素级细线越过视口边缘；fade 渐变在边缘是「单点透明 stop」（0.0f 处 alpha 恰为 0），只掩到精确边界，紧邻边界的像素仍带原色残留。解法——蒙层两端各保留一段完全透明区间（edgeGuard），边缘内容硬性掩为透明，杜绝原色细线。注：该 verticalFadeMask 为 LyricsPanel 共用，首页竖屏/横屏/音乐面板三处同步生效。
  - 验证：`./gradlew :app:compileDebugKotlin` BUILD SUCCESSFUL。


- 修复音乐面板歌词「设置 5 行只显示 3 行」，行高估算与实际渲染不同口径。
  - 做了什么：MusicPanelLyrics 的 `lyricLineHeightPx`（标准单行高，决定视口上限与占位行高）由裸 `TextStyle(fontSize, fontWeight)` 测量改为 `LocalTextStyle.current.merge(TextStyle(fontSize, fontWeight))`，与实际渲染的 LyricChar/Text 同口径（两者均沿用 LocalTextStyle）。
  - 踩坑：现象——音乐面板设 5 行只显示约 3 行；有换行/译文歌词或滚动时可见 4-5 行。原因——主题 Typography 的 bodyLarge 自带 `lineHeight = 24.sp`，而歌词 Text 组件以 `LocalTextStyle.current.merge(TextStyle(fontSize))` 渲染，lineHeight 未被覆盖，故实际每行恒为 24sp 高；但 `lyricLineHeightPx` 只按裸 TextStyle 测（12sp 约 14-16sp），导致估算行距远小于实际行高。视口按偏小行距固定 N 行，实际更高的行溢出被 `clipToBounds` 裁掉，可见行数少于设定值；换行行自身撑成多行、滚动时 buffer 行短暂过窗，故观感又「多」。解法——测量时合并 LocalTextStyle，使视口行距与实际行高一致，N 行设定即显示 N 行。
  - 验证：`./gradlew :app:compileDebugKotlin` BUILD SUCCESSFUL。
  - 备注：首页竖屏 PortraitPlayer.kt L141-145 的 `lyricLineHeight` 亦用裸 TextStyle 估算歌词区高度，属同源潜在问题，待用户确认是否一并修复。


- 歌词行间距独立配置并收紧：不再与其他间距共用同一字面量。
  - 做了什么：MusicPanelLyrics 中相邻歌词行的纵向间距由 3 处重复的硬编码 `2.dp`（视口高度计算、拖拽兜底行距、LyricColumnLayout 行间 spacingPx）收敛为单一常量 `LYRIC_LINE_SPACING`，与行内上下内边距（4.dp，独立含义）解耦，可单独调整；并将值由 2.dp 收紧为 1.dp，缓解歌词显示过于松散、影响阅读连贯的问题。
  - 踩坑：无。三处 `2.dp` 语义一致（都是行间间距），但以魔法数字分散在三处，改一处易漏其余导致行距换算不一致；收敛为常量后三处同源，视口高度、兜底行距与纵向布局的行距基准必然一致。
  - 验证：`./gradlew :app:compileDebugKotlin` BUILD SUCCESSFUL。


- 修复暂停播放时歌词已唱完的高亮回退一段，与暂停瞬间实际进度不一致。
  - 做了什么：MusicPanelLyrics 与 MiniPlayerBar 两处歌词跟随循环的「暂停」分支，由直接 `lyricPosition = candidate`（controller.currentPosition，优先 mediaController）改为复用容差逻辑：仅当 candidate 前移（正向 seek）或 `lyricPosition - candidate > LYRIC_SEEK_TOLERANCE_MS`（手动大幅拖动）时跟随，否则保留本地已推进位置。
  - 踩坑：现象——暂停瞬间歌词已点亮的部分回退成未唱色，与停下的真实进度错位。原因——播放中 lyricPosition 按 50ms 真实流逝时间推进，而 controller.currentPosition 由全局 ticker 以 200ms 轮询回报、天然滞后；暂停走 else 分支时无脑 `lyricPosition = candidate`，把准确位置拉回滞后的控制器位置，低于已唱完词的词终点后 isWordSung 变 false、fillFraction 归零，高亮回退。解法——暂停分支沿用与播放分支一致的容差判定，把「控制器滞后」与「真正 seek」区分开，仅在小幅落后时保留本地真实位置；正向 seek（candidate ≥ lyricPosition）与大幅回退仍正常跟随。
  - 验证：`./gradlew :app:compileDebugKotlin` BUILD SUCCESSFUL。

- 修复歌手歌单详情曲目少于列表展示（多歌手被误过滤）。
  - 做了什么：PlaylistGroupTracksPage 艺术家分流的详情曲目由 `libraryTracks.filter { it.artist == group.name }`（按整串精确匹配）改为 `resolveTracks(libraryTracks, group.trackIds)`（按分组保存的 id 集合解析），与分组列表展示完全一致。
  - 踩坑：现象——歌手歌单列表显示某歌手 8 首、点进详情仅 2 首。原因——前天修多歌手时仅改了列表侧（artistGroups 改按 parseTrackArtists 拆分归组），详情侧 PlaylistGroupTracksPage 仍按 `it.artist == group.name` 整串匹配；多歌手曲目 artist 为整串「A / B」，不等于拆出的歌手名「A」，被整串过滤掉，只剩单歌手/整串恰相等曲目。解法——详情改按 group.trackIds 用 resolveTracks 解析，两侧共用同一份归属，杜绝二次过滤；专辑分支仍按 albumId 过滤不受影响。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 修复横屏首页模态弹窗后系统状态栏常驻不隐藏。
  - 做了什么：MainActivity.updateSystemBarsVisibility 由「在焦点/配置变化回调中立即 hide/show」改为「经 window.decorView.post 延后到窗口过渡结束后再执行」，并在横屏隐藏系统栏时按「隐藏全部 / 仅隐藏状态栏 / 显示全部」三态分派。长按上一曲/下一曲唤出的调速对话框等独立 Dialog 窗口（DialogCard → androidx.compose.ui.window.Dialog）弹出期间系统会强制显示系统栏，关闭后焦点回归时立即 hide 可能被窗口切换过程覆盖；post 后稳定落回预期显隐。
  - 踩坑：现象——横屏长按唤出播放速度弹窗等 Dialog 后，状态栏一直显示不隐藏。原因——弹窗是独立窗口，打开即强制显示系统栏；关闭后 onWindowFocusChanged(true) 里立即调用 insetsController.hide 与系统窗口切换过程竞争，hide 未生效。解法——hide/show 统一延后到 decorView.post 的下一轮消息队列执行，避开窗口切换窗口期；同时把原「横屏隐藏全部、竖屏按需隐藏状态栏」的逻辑改写成三态分派，避免 else 分支误把横屏仅需要隐藏状态栏的场景显示全部。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 首页横屏弹出播放列表时自动隐藏标题栏与控制栏；返回键优先收起播放列表。
  - 做了什么：①ExpandedAssembly 新增 LaunchedEffect(panelState.playlistVisible)：播放列表面板展开时把横屏统一显隐的 chromeVisible 置 false，标题栏/控制栏随之自动收起，避免遮挡面板。②LandscapePlayer 新增 BackHandler(enabled = playlistVisible)：播放列表面板打开时系统返回键优先收起面板，不再落到首页退出/双击返回提示逻辑（此前横屏无此 BackHandler，返回键直接触发 AppNavHost 的退出逻辑）。
  - 踩坑：①现象——横屏播放列表（底部弹层）展开后标题栏/控制栏仍悬浮显示遮挡内容，且按返回键不是收起列表而是触发双击退出提示。原因——横屏只有 PortraitPlayer 有播放列表的 BackHandler，LandscapePlayer 缺失；且 chromeVisible 与 playlistVisible 相互独立、未联动。解法——补 LandscapePlayer 的 BackHandler（BackHandler 以组合更深者优先，横屏下会先于 HomeScreen/AppNavHost 的返回逻辑拦截），并在 ExpandedAssembly 用 LaunchedEffect 监听 playlistVisible 联动收 chrome。②返回键优先级：确认 Compose BackHandler 按组合层级后进者优先，LandscapePlayer 最内层，故能正确先关列表。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 修复歌手歌单不解析多歌手：多歌手曲目（如 "A / B"）不再被当作单一歌手。
  - 做了什么：PlaylistLogic.artistGroups 由 `all.groupBy { it.artist }`（按整串分组）改为逐曲 `parseTrackArtists(track.artist)` 拆分，多歌手曲目同时归属到每位歌手名下；distinctArtistCount 由 `all.map { it.artist }.distinct()` 改为 `all.flatMap { parseTrackArtists(...) }.distinct()`。PlaylistSort.parseTrackArtists 由 private 改为 internal 供复用（多歌手分隔符逻辑与默认排序保持一致）。组 key 维持 `artist:<歌手名>`，空歌手仍回退 unknownArtist 文案。
  - 踩坑：现象——歌手歌单把 "A / B" 与 "B / C" 视为两个不相关歌手，且 "A / B" 与 "A / C" 不共享歌手。原因——artistGroups 直接按 artist 整串 groupBy，未拆分多歌手；而 PlaylistSort 默认排序早已有 parseTrackArtists 拆分逻辑但为 private 未复用。解法——上提 parseTrackArtists 为 internal，分组改为解析后归并；因 PlaylistGroup 按 trackIds 引用、key 独立，同一曲目进多个歌手组不冲突。
  - 验证：`./gradlew :app:compileDebugKotlin` BUILD SUCCESSFUL。


- 首页播放列表高度按朝向自适应：竖屏减半、横屏全高。
  - 做了什么：PlaylistSheet 由固定 `fillMaxHeight(0.7f)` 改为按朝向取分：竖屏 0.5f（高度减半）、横屏 1f（全高）。复用 `rememberWindowLandscape()`（窗口实测宽高比判朝向）计算 `isPortrait` → `sheetHeightFraction`。不改内容布局，仅高度比例随朝向变化，横屏与竖屏共享同一套 UI 面板。
  - 踩坑：①沿用 HomeScreen 已用的 `rememberWindowLandscape()` 而非另写，保证与首页全局朝向判定一致（横屏时首页隐藏系统栏的沉浸逻辑也依赖它）。②`fillMaxHeight` 取值以 `sheetHeightFraction` 直接替换字面量 0.7f，其余修饰符（背景圆角、imePadding、底部对齐）不变，减少横屏全高下的底部弹出动画与布局联动风险。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 默认排序归属冲突：多歌手曲目按「歌最多者」归属，避免把多个歌手块并入一块。
  - 做了什么：PlaylistSort.sortedByDefaultOrder 由「歌手连通块（BFS）」改为「归属歌手」。对曲库（整个待排序列表）统计每位歌手曲目数 artistSongCounts；每首多歌手曲目归属到其中曲目数最多的歌手，数量相同取解析顺序靠前者（ownerOf，仅当候选 > 当前才替换）。锚点之下按归属歌手统计曲目数：≥2 首归「歌手块」（二级），1 首归「专辑块」（三级，跨歌手同专辑仍可相邻）。彻底消除多歌手把 A、B 两个歌手块经由一首「A / B」合并成一大块的冲突。
  - 踩坑：①现象：上一版用歌手连通块（共享任一歌手即同块），当某曲目为「A / B」且 A、B 在曲库各有数首时，会把 A 块与 B 块经由此曲目连通合并成一块——与本意「各自聚拢」冲突。原因：连通块是传递性聚合，多歌手曲目同时钩住多个歌手块。解法：改用归属歌手（owner），多歌手曲目只归其一，杜绝一歌占多块。②归属计数范围：以曲库整表统计（含锚点，符合「曲库中数首」），而「成块判定」以锚点之下归属歌手数量为准，两者分离——计数定归属、锚点下数量定二级/三级。③平票取解析顺序靠前者：用「仅当严格大于才替换」实现，与历史 parseTrackArtists owner 归属语义一致。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 默认排序补多歌手解析：多歌手曲目不再按整串当单一歌手。
  - 做了什么：PlaylistSort.sortedByDefaultOrder 由「按 it.artist 整串计数聚拢」改为「按解析出的歌手连通块聚拢」。新增 parseTrackArtists + ARTIST_SEPARATOR（`[、,，;；/\\&]`）拆分多歌手；对锚点之下曲目建「歌手→曲目下标」索引，BFS 求歌手连通块——共享任一歌手的曲目归同一块；连通块 ≥2 首进「歌手块」（二级），单首（不与他人共享歌手）进「专辑块」（三级）。多歌手如「A / B」与「B / C」因共享 B 归同块相邻，不再被整串差异拆开。
  - 踩坑：①现象：本地/在线多歌手曲目 artist 为整串（API 侧 `joinToString(" / ")`），原先按 `it.artist` 分组把「A / B」当作一个原子歌手，「A / B」与「B / C」虽共享 B 却分属不同组。原因：未做多歌手拆分。解法：先 parseTrackArtists 拆出个人歌手，再用连通块定义「共享任一歌手即同块」。②连通块用 BFS 而非直接按歌手名分组——多歌手曲目可能属多组，需把共享歌手的曲目互相连成块（传递性），块内保持标题序、块序沿用标题序首现顺序（stableGroups）。③块归属以连通块大小判定：≥2 首为歌手块、1 首为专辑块，与单歌手场景等价，多歌手仅是让「共享歌手」的判定更准确；专辑块键仍用专辑名+id，跨歌手同专辑仍可相邻。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 修复默认规则排序两处异常：同专辑未相邻、标题自然序首位偏移。
  - 做了什么：PlaylistSort.sortedByDefaultOrder 由「标题排序 → 按歌手分块 → 每个歌手块内再按专辑分块」改为「锚点 + 双趟聚拢」。以标题自然序首位歌曲为固定锚点（listOf(anchor) 置于队首，不参与后续聚拢，位置恒定）；锚点之下统计各歌手出现次数：歌手出现 ≥2 首的曲目归入「歌手块」（二级，按歌手聚拢，块内保持标题序），歌手仅 1 首的曲目归入「专辑块」（三级，按专辑名+id 聚拢，可跨歌手相邻）。通过分组键加前缀 `A\u0000`（歌手块）/`L\u0000`（专辑块）保证两类块互不串并，且被二级歌手块占用的曲目不再参与三级专辑聚拢。stableGroups 按首现顺序稳定分组、块内保持原序，故标题自然序首现顺序仍整体主导。
  - 踩坑：①现象：原实现在歌手块内才按专辑聚拢，跨歌手的同专辑曲目无法相邻；且按歌手分块会把标题自然序首位歌曲挪走。原因：层级嵌套把「专辑相邻」限制在单一歌手块内，且锚点未从聚拢流程中剔除。解法：锚点抽离固定 + 歌手块与专辑块在锚点下按出现次数互斥划分，专辑块天然允许跨歌手。②分组键需区分两类块，否则某歌手名恰与某专辑键同名会误并；用 `A\u0000`/`L\u0000` 前缀隔离。③锚点不参与歌手计数——若首位歌曲为某歌手的唯一锚点，该歌手其余歌曲在锚点下计数会少 1，是否成块以锚点下实际数量为准，符合「锚点下聚拢」语义。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 评审「系统自带封面解析是否比自实现手动解析器更快、是否有必要迁移到系统 API」。
  - 结论：项目封面解析本就基于系统 API（`MediaMetadataRetriever.embeddedPicture` / MediaStore albumart URI / `ContentResolver.loadThumbnail`），并无逐字节手写封面解析器；所谓「手动」部分是 MusicScanner.loadAlbumArt 读内嵌原图（≤2048）→ ImageDecoder 解码 → WEBP 重编码 → SHA 去重落盘 的一次性重链路，以及 HomeAlbumArt 无缓存时逐次 MediaMetadataRetriever.setDataSource 的实时兜底。
  - 论断是否属实：部分属实。对已入库本地文件，系统内建封面（loadThumbnail/albumart）由 OS 扫描生成并常驻缓存、直接返回已解码小图，确比读全量内嵌+解码+重编码更快。
  - 迁移必要性：不宜整体迁移。①系统缩略图固定 256px，首页全宽沉浸大封面 256 上采样会糊（MusicScanner L169-170 已明确）；②MediaStore 仅索引标准媒体目录，fromUri 导入曲目（path="" albumId=0）与在线流无条目，系统 API 取不到；③封面解析发生在 enrich 一次性，慢的是扫描批处理而非每次显示。更合理方向是「分层复用系统缓存、保留高质量兜底」：补全阶段优先用系统 albumart/thumbnail 覆盖低画质场景，仅对大封面/无索引文件走内嵌全量。
  - 踩坑：无（评审阶段）。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL（评审后已落地，见下条）。

- 落地分层优化：封面补全改为「系统专辑封面优先、内嵌全量兜底」。
  - 做了什么：MusicScanner.loadAlbumArt 的三层优先级由「内嵌 → 专辑封面 → 缩略图」改为「系统专辑封面 → 内嵌 → 缩略图」。已入库曲目（albumId>0 且 MediaStore 已提取封面）直接命中系统专辑封面 URI，免去逐首 MediaMetadataRetriever.setDataSource 打开文件的 I/O——这是批量补全的主要成本；仅当无系统专辑封面（albumId<=0 的外部导入、MediaStore 未提取、专辑内单曲封面各异的合集）才回退到内嵌全量提取；缩略图仍作最后兜底。画质不受影响：MediaStore 专辑封面是原图（非 256px 缩略图），仍按 ≤2048 采样解码。AlbumArtSource 仅在 loadAlbumArt 内标记、下游 enrichLocalCover 只读 bitmap，重排安全。
  - 踩坑：①重排前先核实 AlbumArtSource 是否被下游消费——grep 确认仅在 loadAlbumArt 内赋值、无任何读取点，故重排不影响缓存归属；②保留内嵌兜底而非完全弃用，正是为了覆盖 albumId<=0 外部导入与合集单曲封面各异两类场景，否则会丢封面或误用共享专辑图。③UI 实时路径 HomeAlbumArt 的 loadEmbeddedCover 维持内嵌原图不动（首页大封面需要高分辨率），本次仅优化补全阶段。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 列表略缩图解耦：PlaylistArt 改为直接走系统 MediaStore 缩略图缓存，大封面保持原路线。
  - 做了什么：MusicPanelCover.PlaylistArt 对 MediaStore 索引曲目（audioUri 以 content://media/ 开头）改用 `ContentResolver.loadThumbnail(Size(256,256))` 经 produceState 直接渲染，首帧命中 OS 常驻缩略图缓存，不再等待全量解析器逐首读内嵌→解码→转码→落盘；非索引曲目（外部导入/在线）与系统缩略图读取失败时回退到既有 coverModel 磁盘缓存/占位符。LaunchedEffect 的 requestMetadata 保留（仍为大封面/歌词补全）。AlbumArt/HomeAlbumArt（DiscArt 迷你播放器、96dp 刷新、轮播、首页沉浸大封面）维持 coverCachePath/内嵌原图的高清路线不变。
  - 踩坑：①改造前逐一核查 PlaylistArt 全部调用点尺寸（22/28dp 歌单行、列表行、选择器、迷你面板），均属小略缩图，256px 系统缩略图清晰度足够，无放大模糊风险；大尺寸封面用的是 AlbumArt/HomeAlbumArt，故仅改 PlaylistArt、不动另两者。②loadThumbnail 仅对 MediaStore 索引内容 URI 有效，外部导入（DocumentsContract）与在线曲目不含 content://media/ 前缀，必须保留缓存兜底，否则这些曲目会丢封面。③`if (indexed)` 内 audioUri 已被 Kotlin 数据流分析智能转型为非空，无需 `!!`，去除避免编译告警。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 音乐面板/迷你播放器封面切系统略缩图：AlbumArt 与 DiscArt 一并走系统略缩图，大封面仅首页三处保持内嵌原图。
  - 做了什么：MusicPanelCover 抽出私有 `SystemCoverArt(track, modifier, thumbnailSize, placeholderIconSize)`（MediaStore 索引曲目走 `loadThumbnail` 即时命中缓存，非索引/失败回退磁盘缓存或占位符）；AlbumArt 与 PlaylistArt 均改由它渲染。因 DiscArt 默认 coverArt 即 `AlbumArt`，迷你播放器自动随动。AlbumArt 用 512px 请求（轮播居中封面最大约面板高度 55%，需保证清晰度），PlaylistArt 用 256px（列表行小图）。最终分层：HomeAlbumArt/HomeImmersiveCover/LandscapePlayer 三处首页大封面维持 coverCachePath/内嵌原图高清链路，其余（迷你播放器、音乐面板、刷新预览、轮播、列表行、歌单行、选择器）全部走系统略缩图。
  - 踩坑：①逐点核对 AlbumArt 三个调用点显示尺寸——DiscArt 迷你播放器（64dp 黑胶）/刷新预览 96dp 是小图，但 MusicPanelCoverCarousel 居中封面 `minOf(landscapeCoverWidth, maxHeight*0.55)` 可达数百 dp，若沿用 256px 会放大发糊，故 AlbumArt 单独用 512px 请求；PlaylistArt 全部调用点（22/28dp 歌单行、列表行、选择器）均为小图，256px 足够。②共用 SystemCoverArt 后占位符图标尺寸不同（AlbumArt 24dp / PlaylistArt 12dp），故抽成参数而非硬编码。③DiscArt 无需单独改——其默认 coverArt 委托 AlbumArt，改 AlbumArt 即覆盖迷你播放器。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

- 系统媒体面板封面改走系统专辑封面 URI（为踩坑填回）：不再向系统侧暴露大图缓存封面。
  - 做了什么：MusicPlayerHelper.toMediaItem 的封面由「一律 MusicCoverProvider 暴露 coverCachePath 大图」改为「MediaStore 索引曲目（albumId>0）直接用 `content://media/external/audio/albumart/<albumId>` 系统专辑封面 URI，非索引曲目（albumId<=0 外部导入/在线）回退自定义 Provider」。系统媒体面板（通知栏/锁屏）本就以小圆图展示，用系统 MediaProvider 提供的封面并带系统缓存，符合场景，也省去系统侧读取应用高分辨率缓存封面文件。
  - 踩坑：①setArtworkUri 指向的必须是「可当作图片解码的 URI」——不能直接指向音频 MediaStore URI（那是音频元数据不是图），系统面板会按图片解码失败；MediaStore 的专辑封面 albumart URI 才是返回图片的系统资源。②albumart 是专辑级封面（合集曲目共享），且若 MediaStore 未提取到封面会显示空——这与列表/面板走 loadThumbnail 同一语义，符合「锁屏跟随系统」的预期；有手动在线封面但文件无内嵌的索引曲目在锁屏会丢失该封面，属可接受边界。③改动点仅 toMediaItem 一处（grep 确认 setArtworkUri/buildUri 只在此使用），无其他牵连。
  - 验证：`./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug` 均 BUILD SUCCESSFUL。

## 2026-09-12

- 首页播放列表新增排序功能（刷新/关闭之间插入排序按钮，复用主题选择对话框风格）。
  - 做了什么：PlaylistSheet 头部在刷新与关闭按钮之间插入 AppIcons.Sort 按钮，点击弹出 PlaylistSortDialog（AlertDialog + 选中高亮 + 顶部「逆序」开关）。排序字段：默认顺序 / 文件修改时间 / 标题 / 歌手 / 专辑 / 时长；点选字段即时应用并关闭，「逆序」开关即时生效且不关闭对话框。新增 PlaylistSort.kt 收敛排序枚举与比较器（把原 MusicPlaybackState 私有 sortedByDefaultOrder/parseTrackArtists 迁入）；标题/歌手/专辑及默认顺序的文本比较统一内置首字母自然序——系统 Collator(Locale.CHINA) 中文按拼音、英文按字母，首字符为数字（含中文数字）者统一靠前（不单列选项，作为字段自身逻辑）。MusicTrack 新增 fileModifiedMs，MusicScanner 读 MediaStore DATE_MODIFIED（秒→毫秒），列表 JSON 缓存同步读写。排序规则存入 music_playlist_cache_preferences 随列表一并落盘，restoreSavedState / PlaylistRefresher.refresh / switchToPlaylistQueue 在默认全量播放列表时套用；仅 playlistSource==null 时可点，切到自定义/智能歌单时按钮置灰，避免覆盖歌单自身拖拽顺序。文案中英同步，AppIcons 新增 Sort。
  - 踩坑：①比较器链式调用有两处类型陷阱：`compareBy(String.CASE_INSENSITIVE_ORDER){...}.thenBy{...}` 触发 Kotlin 类型推断失败（Cannot infer type parameter 'T'），`thenBy(comparator)` 也不存在（报 actual type is 'Comparator'，but '(T) -> Comparable<*>?' was expected）——比较器相接要用 `then(comparator)`，`thenBy` 只接受选择器 lambda；最终统一为 `naturalStringComparator<T> { selector }` 泛型比较器（显式 `<MusicTrack>` 指定类型）规避。②Collator 非线程安全，故按比较器实例创建而非全局共享，排序放 Dispatchers.Default 执行。③排序读取 fileModifiedMs，旧缓存该字段为 0，需刷新一次补齐（在线/外部曲目恒为 0，排末尾）。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- 排序功能细化：默认顺序确认、逆序开关改位、文件修改时间更名「新旧」。
  - 做了什么：①默认顺序改为「分趟稳定聚拢」以化解冲突：第一趟按标题自然序（数字优先）主排，第二趟按歌手把曲目聚拢为连续块（块先后沿用标题序的首现顺序），第三趟在每个歌手块内按专辑再聚拢；块内均保持标题序，故同歌手相邻、同专辑相邻，且不为满足相邻而牺牲标题主排。实现为 `sortedByDefaultOrder` = 标题排序 → `stableGroups{artist}` → 每个歌手块内 `stableGroups{albumName+albumId}`。移除原按歌手数量归并（owner 归属）与一次性 title→artist→album 比较器。②逆序开关由对话框内 Switch 行改为置于「排序方式」标题右侧的小字（13sp）可点击文案，点击即时切换方向且不关闭对话框；文案显示与当前方向相反的目标方向——正序显示「逆序」、逆序显示「正序」。③「文件修改时间」更名「新旧」，排序改为仅按 fileModifiedMs 排序（新在前，compareByDescending），去掉标题自然序兜底，不引入其它排序机制。文案中英同步（新增 music_panel_sort_ascending，descending 英文由 Reverse order 改为 Descending）。
  - 踩坑：①「标题全局升序」与「同歌手/同专辑相邻」是互斥诉求——只要标题做主键，标题不同的同歌手曲目必被其他歌手插开，单次比较器无法同时满足（首版按 title→artist→album 比较器实现，次级的歌手/专辑归并形同虚设）。解法是拆成多趟稳定操作：先排序、再聚拢，各趟保持上一趟相对顺序，冲突消除。②聚拢键需含 albumId（`albumName\u0000albumId`），否则同名不同专辑会被并成一块。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- 更新双语 README 并产出 v3.3.0 发布文案。
  - 做了什么：README.md / README.zh-CN.md 的「特性 / Features」与「页面 / Screens」两处补充「歌词区上下拖拽调整播放进度」；致谢一栏未改动；产出按「新增 / 优化 / 修复」三分块的发布文本。
  - 结论：致谢不加新参考——本轮歌词滚动与拖拽调进度都是本项目内实现（窗口化布局 + 浮点行号动画 + 实测行高换算的手势几何），与参考示例的 LazyColumn + animateScrollBy 是两套不同方案，未移植其代码或素材；YRC「字标签是绝对时间」是对公开格式规范（Lyricify / AMLL 文档）的核对，属协议事实而非代码引用；歌词链路既有的 Qplayer 致谢已覆盖该渊源。
  - 踩坑：应用内更新对话框把 release body 当纯文本渲染（UpdateDialog 直接用 Text 显示 changelog），发布文案不能带 Markdown 标记（`##`、`**`），否则弹窗里会露出原始符号。
  - 验证：纯文档改动，不触发编译。

- 修复逐字歌词「整句完全不亮」：YRC 字标签的绝对时间被重复叠加了行首时间。
  - 做了什么：NeteaseMusicApi.parseYrc 的字起点由 `行首 + 字标签` 改为直接取字标签的绝对时间（仅当小于行首时才按相对偏移补上行首，兼容个别源），与 YRC 规范 `[start,duration](start,duration,0)Word` 一致；MusicMetadataCache 新增 `repairLegacyWordStarts`，解析增强 LRC 缓存后修复历史上被写坏的字起点：字起点晚于下一行起点（末行则晚于行首×2）即落在本行区间之外、不可能成立，回退减去行首还原；数据正常时不改动任何内容。
  - 踩坑：①这个 bug 被原实现的「整行时长按词数均分」渲染方式掩盖了——那时渲染根本不读 `word.startMs`，一旦改成按字时序渲染就整句不亮，属于改动暴露的存量缺陷。②歌词缓存按增强 LRC（`<mm:ss.xxx>`）落盘，写坏的绝对值已存在于用户设备上，只修解析对旧缓存无效，必须同时做读取侧修复。③修复判据必须带 `nextLineStart > line.timeMs`，否则重复时间戳的行会被误判并被改坏。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- 修复逐字歌词未按字/词自身时序渲染（原先按「整行时长 ÷ 词数」均分）。
  - 做了什么：LyricWordText.kt 的 `WordSplitLyricText` 改为直接用 `LyricWord.startMs/durationMs` 计算每个词的起止区间：`isWordSung = positionMs >= wordEnd` 整词高亮，`isWordCurrent = positionMs in [wordStart, wordEnd)` 词内逐字从左到右亮起并保留弹簧跳动，词间空隙不再被强行点亮。`durationMs` 为 0 时（增强 LRC 只给起点）用下一个词的起点兜底、末词用下一行起点兜底，并 `coerceAtLeast(wordStart + 1)` 防零时长除零。
  - 踩坑：①当时误判了语义：以为 `NeteaseMusicApi.parseYrc` 的 `start + 字标签`（=绝对）是对的、缓存路径的 `<mm:ss.xxx>`（绝对）也对，实际前者才是错的（见上方条目）；渲染层只需按绝对时间比较，两侧时间基准必须一致。②歌词缓存按增强 LRC 序列化（`<mm:ss.xxx>` 只有起点、无时长），回读时 `durationMs` 必为 0，故「下一个词起点兜底」不是可选优化而是必需路径；词起点本身能完整保留，卡拉OK效果不受影响。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- 修复歌词拖拽调进度在暂停态下「对齐释放后不起播」。
  - 做了什么：MusicPlayerHelper.kt 新增 `seekToAndPlay(state, positionMs)`（同一协程内先 seek 再 play）；MusicPanelLyrics.kt 对齐释放的跳转改用它。
  - 踩坑：原实现只调 `seekTo`，暂停态只改播放位置、不恢复播放，故拖动后没声音；又因 seek 与 play 若分两次派发会竞争（play 可能先于 seek 生效，出现从旧位置起播的瞬间），必须合并到同一协程内顺序执行。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- 歌词区支持上下拖拽实时调整播放进度（跟手滚动 / 中线基准标识 / 对齐才跳转 / 未对齐弹性回弹）。
  - 做了什么：MusicPanelLyrics.kt 新增纵向拖拽调进度。①滚动位置改为普通状态 `scrollPosition`（浮点行号）承载：正常播放由跟随动画 `animate(...)` 推进，拖拽时由手势直接写入，释放后的吸附/回弹也写同一状态——用普通状态而非 Animatable，避免释放瞬间读到过期动画值导致内容闪回拖拽前位置。②新增 `LyricScrollGeometry`（普通对象，非 Compose 状态），由 LyricColumnLayout 测量时写入窗口起点与各行实测高度；手势按「像素 ÷ 当前行到下一行中心的距离」换算行位移，行高不定也 1:1 跟手且不触发额外重组。③中线固定基准标识（左右两段短横线，形如「- 歌词 -」，仅拖拽时淡入），拖拽中高亮中线最近的候选行作为选中反馈。④释放判定：`|位置 - round(位置)| ≤ 0.35 行` 则吸附到该行并 `seekTo(lines[i].timeMs)`；否则不跳转进度，以 `spring(DampingRatioMediumBouncy)` 弹性回弹到拖拽前位置。⑤手势用 `detectVerticalDragGestures` 声明在 `combinedClickable` 之后（内层），过 slop 后 `consume`：拖拽取消点击，且不触发外层左右滑动切面板；点按/长按不受影响。⑥全程不暂停、不改播放状态，只有对齐释放那一刻才 seek。
  - 踩坑：①对齐释放时直接写 `lyricPosition` 后，原位置跟踪循环的 1500ms 回退容差会把小于 1.5s 的向后跳转当抖动忽略，导致向后拖拽定位失效；新增 `seekGuardUntilMs` 短时保护窗，窗内本地进度自走、忽略控制器旧位置。②回弹用 `animate` 写普通状态，必须用代数 `settleGeneration` 作废旧动画——被新拖拽打断的旧协程仍会跑完，无校验会把位置/`settling` 改回去。③`scrubbing`/`settling` 需在启动协程前同步置位，否则释放当帧显示会回退到跟随动画的旧值。④手势回调按值捕获 `lines` 会在歌词异步补全后失效，改用 `rememberUpdatedState`。⑤`animate(initialValue, targetValue, animationSpec)` 第三个位置参数是 `initialVelocity`，须用命名参数传 spec。⑥窗口锚点由固定 `activeIndex` 改为 `round(显示位置)`，拖拽时窗口随内容滚动；沿用 buffer=2 保证进出窗口的行都在视口外。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- 歌词滚动改为连续上移，并降低非当前行不透明度（参考 示例/BetterNCM-Android-master 的歌词滚动逻辑）。
  - 做了什么：MusicPanelLyrics.kt 移除 AnimatedContent 的整块替换（旧实现出窗只上滑 1/4 高度、入窗从整屏高度滑入，观感是「换页」而非滚动），改为「窗口上下各多渲染 2 行 + 动画浮点行号驱动连续平移」：新增 `Animatable<Float> animatedIndex`，当前行变化时以 `tween(400ms, FastOutSlowInEasing)` 过渡；`LyricColumnLayout` 按实测行高把动画行号对应位置对齐视口中线（相邻两行中心之间线性插值，跨行不跳变）；行以 `key(index)` 渲染，窗口平移时保留同一行的高亮状态；非当前行按 `emphasis` 插值到 0.55 不透明度（随高亮进度平滑过渡）；跨度 >2 行（拖动进度/切歌）直接 `snapTo` 定位，并用 `displayPosition` 避免动画归位前把窗口内错误行居中。
  - 踩坑：①窗口 buffer 起初取 1，推算后发现被移除的顶行在换行瞬间仍落在渐隐区内（viewport 上下缘为 ±(offset+0.5) 行），会一闪；按「被移除行须完全在视口外」反推需 buffer≥2（offset+buffer≥offset+1.5），改为 2（渲染 9 行，仍少于旧实现过渡期的 5+5 行）。②窗口锚定当前行、平移量由动画行号换算，二者同时变化时对共同行是等量反向位移，屏幕坐标天然连续（行高不一致也成立），无需额外补偿，这是选择该方案而非逐帧补偿的原因。③读 `Animatable.value` 会按帧重组，属预期（旧实现动画期间同样重新布局）。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- 更新功能新增 APK 完整性校验（SHA-256），避免安装被篡改/损坏的包。
  - 做了什么：UpdateManager.kt 解析 GitHub asset 的 `digest`（`GitHubAsset` 增 `digest`，`UpdateInfo` 增 `sha256`，归一为小写十六进制）；`downloadAndInstall` 在 `STATUS_SUCCESSFUL` 后、拉起安装 Intent 前流式计算已下文件 SHA-256 比对，不符或缺失则删除文件、上报失败、不安装，并 `CrashLogManager` 记录；新增私有 `verifyApkHash`/`sha256Hex`（64KB 流式，避免整包入内存）。按用户决策：**取不到期望哈希即拒绝安装**（fail-closed）；失败复用现有通用提示，不新增文案。
  - 踩坑：①原以为需改发布流程补校验文件，实为 GitHub Releases API 自 2025-06 起已在 asset 上返回 `digest: "sha256:..."`，与现有 `releases/latest` 同一响应，零额外请求即可获得期望哈希。②校验必须置于 FileProvider/安装 Intent 之前，失败同时删除已下文件，否则残留 APK 仍可能被手动安装。③fail-closed 的代价：2025-06 之前发布、无 digest 的 release 将无法走应用内更新（需先发一次带 digest 的版本过渡）。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- 执行待处理项第 2 项：更新检查一次性事件收口，并修复自动检查误弹提示。
  - 做了什么：UpdateViewModel.kt 将 `MutableStateFlow<CheckFeedback?>` + `clearCheckFeedback()` 改为无回放 `MutableSharedFlow<CheckFeedback>`（`messages`），并在 `checkForUpdate` 中仅在 `force=true`（手动）时 `emit`；AppContent.kt 改为 `LaunchedEffect(upToDateText, checkErrorText) { messages.collect { Toast } }`（以文案为键重建收集，语言切换后不再弹旧语言文案），移除可空状态与手动清理。
  - 结论：原实现无论自动/手动检查，只要「无新版本且无错误」即置 `_checkFeedback=UP_TO_DATE`，导致每日首次回前台（ON_RESUME）误弹「已是最新」；本次一并修复。
  - 踩坑：注意全局收集位置与模板不同——模板在设置页收集、首页 `checkDailyOnce` 无订阅者故自动检查天然静默；本项目在 AppContent 全局收集，故不能只换 SharedFlow，必须同时以 `force` 门控发射，否则自动检查仍会 Toast。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- 将「播放状态不改 StateFlow<UiState>」的决策写入开发规范，避免重复决策。
  - 做了什么：在 docs/开发规范.md「状态管理」段补充两条——①明确「页面状态统一 StateFlow<UiState>」的约束对象为 screens/ 页面层（ViewModel 持 MutableStateFlow、对外暴露 StateFlow）；②明确跨页面/悬浮窗/Service 的共享状态持有者（如播放状态）不属页面状态，应作为 StateHolder 收敛为单一持有者、用 Compose 快照状态暴露（保留细粒度重组 + 支持 Service 等非 Compose 上下文同步读取），一次性事件仍走 Channel/SharedFlow，禁止改为单体 StateFlow<UiState>。
  - 踩坑：无。
  - 验证：纯文档改动，不触发编译。

- 评估「播放状态专项（StateFlow<UiState> 重构）」收益，结论：收益不明显，不执行。
  - 做了什么：审查 docs/待处理项.md 第 1 项，结合 MusicPlaybackState.kt（约 61 处 Compose 可变状态）、消费面（42 个文件、数百处引用）、Service 与悬浮窗生命周期，判断是否值得重构。
  - 结论：①规范「页面状态统一 StateFlow<UiState>」针对页面层（screens/ 下 XxxViewModel+XxxUiState），项目已在该层落实（HomeViewModel 用 MutableStateFlow<HomeUiState>）；MusicPlaybackState 是跨首页/悬浮窗/Service 的共享状态持有者，不属该条款。②当前 mutableStateOf 即 Compose 官方 StateHolder 模式，配 snapshotFlow 细粒度重组；改单体 StateFlow<UiState> 会让 5Hz 进度更新触发整棵订阅树重组（除非再拆多 flow，复杂度更高）。③规范真正诉求（稳定唯一键、写入收口）已满足：列表项以 MusicTrack.id 为键，UI 写入已部分收口到 setXxx 意图方法。④成本/风险高，且无法消除持有者本身可变性（player/jobs/Mutex/cache）。故保持现状，不改动。
  - 踩坑：无。
  - 验证：无代码改动，不触发编译。

- 新增 docs/待处理项.md，汇总本轮未处理/暂缓项供新对话推进。
  - 做了什么：整理播放状态专项（MusicPlaybackState 66 处可变状态，规范冲突，改动面最大）、一次性事件收口（update/UpdateViewModel 的 checkFeedback 改 SharedFlow/Channel）、目录取舍（service/、floatingwindow/、data/ 细分）、其它遗留（PlaylistStore 可变状态、启动语言回退取舍）；并写入新对话启动须知（docs 阅读、工作日志、验证命令、沙箱外执行 git、git mv/大小写重命名注意）。
  - 踩坑：无。
  - 验证：纯文档改动，不触发编译。

- DI 步骤 2：消除 Composable 对 AppContainer 的直接引用。
  - 做了什么：AppContainer.kt 移除 LocalAppContainer，改为提供具名组合局部（LocalMusicPanelStateHolder/LocalPlaylistStore/LocalMetadataEnricher/LocalPlaylistRefresher/LocalApplication/LocalSettingsRepository/LocalLocalizationManager），并新增 ProvideAppDependencies 统一注入；MainActivity 与两个悬浮窗 ViewManager 改用 ProvideAppDependencies；迁移 16 个消费文件（AppNavHost、HomeScreen、TypographyScreen、SettingsScreen、HomePanelState、PortraitPlayer、HomeAlbumArt、MusicPanelCover、MusicPanelOverlay、OnlineSearchPanel、PlaylistPages、PlaylistPicker、PlaylistPanel、PlaylistSheet、PlaylistSwitcher、LibraryAnalysisSheet）由 container.xxx 改为具名局部；全库已无 LocalAppContainer 引用。
  - 踩坑：页面 ViewModel 工厂（viewModelFactory 的 initializer lambda）不是 Composable，不能在其中读取 `.current`，须先在 Composable 作用域捕获局部值再传入。
  - 验证：./gradlew :app:compileDebugKotlin、./gradlew :app:assembleDebug 均 BUILD SUCCESSFUL。

- DI 步骤 1：AppContent 经 LocalUpdateViewModel 取更新依赖。
  - 做了什么：UpdateViewModel 新增 LocalUpdateViewModel 组合局部（对齐模板 LocalAppUpdateViewModel 范式）；MainActivity 提供该局部；AppContent 由 LocalAppContainer.current.updateViewModel 改为 LocalUpdateViewModel.current，不再引用 AppContainer。
  - 踩坑：无。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 重构步骤 10：消除 MainActivity 主线程 runBlocking。
  - 做了什么：resolveBootLanguage 原在 attachBaseContext（主线程）以 runBlocking 回退读 DataStore，违反"禁止主线程 runBlocking"；改为镜像缺失时回退 AppLanguage.SYSTEM（默认值），移除 runBlocking 与 bootstrapAppLanguage 依赖。行为取舍：首次安装/旧版升级后首次冷启动若用户曾设非系统语言，本次以系统语言渲染，界面层语言流会即时纠正 Compose 文案，Application 预热任务补写镜像使下次冷启动同步命中。
  - 踩坑：无。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 重构步骤 9：更新下载通知文案入资源并按应用语言取用。
  - 做了什么：UpdateManager 下载通知标题/描述由硬编码改为资源（update_notification_title/update_notification_downloading，中英同步）；为满足"非 Compose 上下文须用本地化 Context"，UpdateViewModel 注入 SettingsRepository+LocalizationManager，下载前按当前应用语言构造本地化 Context 再取资源；AppContainer 同步构造参数。
  - 踩坑：无。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 重构步骤 8：AppInfo 用户可见硬编码文案入资源。
  - 做了什么：品牌名、"[日志]"入口、QQ 群标识、分享日志的主题与选择器标题由硬编码改为资源（app_info_brand/log_label/qq_group/share_log_subject/share_log_title，中英同步）；Composable 内经 stringResource 预取后在嵌套函数使用。
  - 踩坑：无。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 重构步骤 7：Clipboard 迁出 utils，复制提示文案入资源。
  - 做了什么：copyToClipboard 由 utils/（规范要求无状态纯函数、无 Android 依赖）迁至 ui/（通用 UI 能力，包 com.yichao.evilgodxu.ui）；硬编码提示 "已复制: $text" 改为资源 copy_success_toast（中英同步）；更新 2 处 import（PortraitPlayer、MusicPanelCover）。utils/ 现仅剩纯函数 TimeFormat。
  - 踩坑：无。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 重构步骤 6：ui/AppContent.kt 上提至根包，并清理遗留空目录。
  - 做了什么：AppContent.kt 由 ui/ 迁至根包 com.yichao.evilgodxu（规范：ui/ 仅收纳 component/、icons/），包名改为根包并移除 MainActivity 中冗余 import；同时删除 Koin/domain 迁移遗留的空目录 di/、domain/ 及迁移后的空目录 data/permission/、ui/section/。
  - 踩坑：无。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 重构步骤 5：data/permission/ 上提为顶层 permission/。
  - 做了什么：PermissionMonitor、OverlayGrantMonitor 由 data/permission/ 迁至顶层 permission/（规范：权限状态统一出口），包名改为 permission；更新 7 个文件共 11 处 import（MusicPanelController、HomeViewModel、SettingsViewModel、ExpandedAssembly、CompactAssembly、HomeDialogs、PermissionDialog）。
  - 踩坑：无。
  - 验证：./gradlew :app:clean :app:compileDebugKotlin BUILD SUCCESSFUL（步骤 4 的纯大小写重命名会污染 Kotlin 增量缓存，导致后续编译报 Unresolved reference 'windowsize'，需 clean 后重编验证）。
  - 备注：步骤 4 的目录大小写重命名会残留 Kotlin 增量缓存脏状态，纯增量编译可能误报成功；此类改动必须 clean 验证。

- 重构步骤 4：windowSize/ 目录名对齐规范为 windowsize/。
  - 做了什么：将 windowSize/WindowSizeClass.kt 迁至 windowsize/，包名改为 windowsize；更新 5 处 import（MainActivity、HomeScreen、TypographyScreen、SettingsScreen）。
  - 踩坑：目录名仅大小写差异，Windows 大小写不敏感，需两步重命名（windowSize → windowSize_tmp → windowsize）才生效。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 重构步骤 3：theme/AppSwitch.kt 归位到 ui/component/。
  - 做了什么：统一 Switch 组件 AppSwitch 由 theme/ 迁至 ui/component/（theme/ 仅保留颜色/字体/主题），包名改为 ui.component；更新 3 处 import（MusicPanelSettings、ProxySource、Playback）。
  - 踩坑：无。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 重构步骤 2：ui/section/GroupCard.kt 归位到 ui/component/section/。
  - 做了什么：将分组卡片容器 GroupCard 迁至 ui/component/section/，包名改为 ui.component.section；更新 5 处 import（TypographyGroups、Appearance、ProxySource、Language、Playback）。
  - 踩坑：`git mv` 目标目录不存在时报 "renaming ... failed: No such file or directory"，需先手动创建目标目录再移动（现象→原因→解法）。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 重构步骤 1：ui/dialog/ 归位到 ui/component/dialog/。
  - 做了什么：将 6 个对话框文件（MusicPanelTimer/Speed/Settings/Search/Rename/DeleteConfirm）迁至 ui/component/dialog/，包名改为 ui.component.dialog；更新 5 个引用文件共 10 处 import（PortraitPlayer、PlayerControls、HomeDialogs、OnlineSearchPanel、MusicPanelOverlay）。
  - 踩坑：命令默认在沙箱内执行，git 写松散对象（.git/objects）被拦截报 "Permission denied"，导致 git add/commit 失败；现象→原因→解法：需经 DesktopCommander MCP 在沙箱外执行 git 写操作。另因 add 未覆盖全部文件，首次提交遗漏 6 个文件的包名改动，已用 --amend 补齐。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 更新双语文档以对齐手动 DI 重构与最新目录结构。
  - 做了什么：①技术栈表「依赖注入」由 Koin 4.2.2 改为手动 DI(AppContainer)；②架构段由"全部由 Koin 组装"改为"手动依赖注入组装——Application.onCreate() 构建一次 AppContainer，持有全部应用级单例并经 CompositionLocal 暴露给界面树"；③项目结构目录树同步实际代码——删除 di/、domain/music/、overlay/、utils/localization/、YiChaoActivity.kt/YiChaoApplication.kt，新增 floatingwindow/、localization/、windowSize/、data/music 下 analysis/download/panel/playback 子目录、data/playlist、App.kt/AppContainer.kt/AppUiState.kt/MainActivity.kt/MainViewModel.kt、screens/typography；④架构段悬浮 UI 描述由 overlay/+ui/music 改为 floatingwindow/+ui/component。英文 README.md 与中文 README.zh-CN.md 逐文件同步修改。
  - 踩坑：
    - 源文件注释仍残留 Koin 旧表述（MusicScanner/PlaylistStore/MusicMetadataCache/AppSettings/MusicPanelStateHolder/MusicHttpClient 共 6 处"由 Koin 以单例管理 / 不走 Koin"）。现象→原因→解法：属 Koin 迁移遗留注释，与手动 DI 现状不符；本次仅按需求更新双语文档，未扩及源码改动，后续如需对齐可单独清理。
  - 验证：纯文档改动，不触发编译。

- 清理源码中残留的 Koin 迁移遗留注释。
  - 做了什么：删除/改写 6 处"由 Koin 以单例管理 / 不走 Koin / Koin 尚未初始化"旧表述——MusicScanner、MusicHttpClient、MusicMetadataCache 三处 object 注释改为"以 object 单例形态提供"；PlaylistStore、MusicPanelStateHolder 两处"由 Koin 以单例管理"改为"由 AppContainer 以单例持有"；AppSettings 冷启动引导注释"Koin 尚未初始化"改为"依赖容器就绪前"，与手动 DI 现状对齐。
  - 踩坑：无。
  - 验证：./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL。

- 修正 README 中过时的"assembly/area 分区架构"描述。
  - 做了什么：审计两个 README,确认唯一旧内容是"分区架构(assembly/area 模式)"段——全库不存在任何 {Name}Area.kt,实际为分形态组装模式:{Screen}Screen.kt 做 compact/expanded 形态分发与跨形态副作用(不承载布局)、{Screen}ViewModel/{Screen}UiState、compact//expanded/ 下的 {Screen}Assembly(按尺寸类+旋转选择)、component/ 语义子目录承载页面专用可组合项;据此重写 README.md 与 README.zh-CN.md 对应段。
  - 踩坑：
    - 审计中发现 docs/开发规范.md 页面目录示例把 content/、dialog/ 列为页面根子目录,与代码实际用 component/(其内含 dialog/ 等)不符,属规范与代码漂移,待用户决定是否对齐后再改。
  - 验证：纯文档改动,不触发编译。

- 将 docs/开发规范.md 顶层 ui/ 示例改为 component/ 统一收纳（与页面级规则一致）。
  - 做了什么：按一致性判断，共享 UI 可组合项统一置于 ui/component/ 并按语义细分（通用对话框归 dialog/ 等），去掉规范中推测性的 popupwindow/sheet/toolbar/toast 独立目录；仅图标资源封装（icons/）保留独立。
  - 踩坑：
    - 【待处理项目变更】本次仅改规范，未做代码迁移：ui/dialog/ 下 6 个文件（MusicPanelTimer/Speed/Settings/Search/Rename/DeleteConfirm）仍与 ui/component/ 平级，应迁至 ui/component/dialog/ 并更新约 10 处 import；ui/section/GroupCard 同为组件亦应收敛进 component/。因预算不足暂缓，后续额度允许时再执行并 ./gradlew :app:compileDebugKotlin 验证。
  - 验证：纯文档改动,不触发编译。

- 消除 docs/开发规范.md 第 14/15 条对 ui/component/ 归属的表述歧义。
  - 做了什么：原第 14 条称共享 UI（含对话框等大类型）进 ui/component/，而第 15 条末句残留旧表述"仅纯原子布局组件统一置于 ui/component/"——两句矛盾。按"作用域"而非"类型"区分修正：跨页复用的可组合项（原子/对话框等）统一进 ui/component/；页面专用可组合项收敛于页面包内 component/，复用于多页面的再上提至 ui/component/。
  - 踩坑：无。
  - 验证：纯文档改动,不触发编译。

- 将 docs/开发规范.md 页面目录规范对齐到实际 component/ 收敛方式。
  - 做了什么：按用户判断（项目实际 component/ 收敛更合理：与全局 ui/component/ 命名统一，页面级组装单元与专用弹窗一并语义收敛，避免 content/ 与 component/ 概念重叠），将规范第 15 条示例子目录由 content/、dialog/（页面根）改为 component/（页面包内统一语义目录，其内再按 dialog/、entry/、playback/ 等细分），并重写页面目录示例为实际设置页结构。
  - 踩坑：无。
  - 验证：纯文档改动,不触发编译。
